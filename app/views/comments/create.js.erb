// Exercise 6.10 : AJAX

$('#new_comment').hide();
$('.comment-form').html("Thanks for providing a review!");

// Exercise 6.14 (remove this) (replaced within product.js:received())
// $('.product-reviews').prepend("<%= j render @comment %>");

$('#average-rating').attr('data-score', "<%= @product.average_rating %>");
$('#average-rating-text').html("<%= 'Average rating: ' + number_to_human(@product.average_rating) %>");

refreshRating();

$('#average-rating').raty( 'readOnly', false );
$('#average-rating').raty( 'score', $('#average-rating').attr('data-score'));
$('#average-rating').raty( 'readOnly', true );

// product scoped comment counters
$('#comment-cnt').html("<%= $redis.get @comment_key %>");
